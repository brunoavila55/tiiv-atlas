version: "2"
sql:
  - engine: postgresql
    queries: db/queries
    schema: db/migrations/00001_init.sql
    gen:
      go:
        package: db
        out: internal/database/sqlc
        sql_package: pgx/v5
        emit_json_tags: true

